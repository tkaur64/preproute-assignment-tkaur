import { Box } from "@mui/material";

const Header = () => {
  return (
    <Box
      sx={{
        height: 72,
        borderBottom: "1px solid",
        borderColor: "divider",
      }}
    >
      Header
    </Box>
  );
};

export default Header;